export default function History() {
    return (
        <div>
            History
            Show as another modal?
            {/*  show all queries history */}
            {/* show state left behind too? */}
        </div>
    )
}
